from collections import defaultdict
import sys

NPREF = 2



def build(infile):
    hashtab = defaultdict(list) 
    prefix = ['\n']*NPREF
    with file as open(infile):
       for line in file:
           


def run():
    hashtab = buid(sys.argv[1])


if __name__ == "__main__":
    run()